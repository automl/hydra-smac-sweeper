defaults:
  - override hydra/sweeper: SMAC
  - override hydra/launcher: submitit_smac_local

hydra:
#  launcher:
#    partition: cpu_short
#    cpus_per_task: 1
#    mem_gb: 1
#    gres: null
#    timeout_min: 10
  sweeper:
    n_jobs: 8
    scenario:
      seed: 42
      n_trials: 40
      min_budget: 1
      max_budget: 1
      deterministic: true
#    smac_class: smac.facade.smac_mf_facade.SMAC4MF
#    smac_kwargs:
#      scenario:
#        run_obj: quality
#        deterministic: true
#      intensifier: ${get_class:smac.intensification.hyperband.Hyperband}  # TODO document in docu how to set smac kwargs / caveats
#      intensifier_kwargs:  # TODO use normal SMAC4AC here
#        initial_budget: 1  # TODO we can only use MF with parallelism right now
#        max_budget: 1
    search_space:  # TODO adjust search space
      hyperparameters:
        x0:
          type: uniform_float
          lower: -5
          upper: 10
          log: false
        x1:
          type: uniform_float
          lower: 0
          upper: 15
          log: false
          default_value: 2
  run:
    dir: ./tmp/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: ./tmp/${now:%Y-%m-%d}/${now:%H-%M-%S}

x0: 3
x1: 4

spurious_var: 3.14